apiVersion: v1
kind: Service
metadata:
  name: go-astrafy-service
  namespace: default
  labels:
    app: go-astrafy
spec:
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080  # Actualiza el targetPort a 8080
  selector:
    app: go-astrafy
  type: LoadBalancer
  loadBalancerIP: ""